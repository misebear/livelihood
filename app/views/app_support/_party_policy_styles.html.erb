<style>
  .party-policy {
    --party-accent: #6d28d9;
    --party-soft: #f5f3ff;
    max-width: 1080px;
    margin: 28px auto;
    padding: 28px;
    border-radius: 28px;
    background: linear-gradient(145deg, var(--party-soft), #ffffff 58%);
    border: 1px solid rgba(15, 23, 42, 0.1);
    color: #0f172a;
  }

  .party-policy--signal { --party-accent: #075985; --party-soft: #ecfeff; }
  .party-policy--arena { --party-accent: #be123c; --party-soft: #fff1f2; }
  .party-policy-eyebrow {
    margin: 0 0 8px;
    color: var(--party-accent);
    font-weight: 850;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .party-policy h1 {
    margin: 0 0 14px;
    font-size: clamp(30px, 5vw, 52px);
    line-height: 1.08;
    letter-spacing: -0.04em;
  }
  .party-policy-lead,
  .party-policy-note,
  .party-policy article p,
  .party-policy li {
    color: #475569;
    line-height: 1.75;
  }
  .party-policy-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
    gap: 14px;
    margin-top: 22px;
  }
  .party-policy article,
  .party-support-card {
    padding: 20px;
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.92);
    border: 1px solid rgba(15, 23, 42, 0.08);
    box-shadow: 0 16px 40px rgba(15, 23, 42, 0.06);
  }
  .party-policy h2 { margin: 0 0 10px; font-size: 21px; }
  .party-policy ul { margin: 0; padding-left: 20px; }
  .party-policy-note { margin: 22px 0 0; }
  .party-policy a { color: var(--party-accent); font-weight: 800; }
  .party-policy code { overflow-wrap: anywhere; }
</style>
